# Test Suite — create-hook

| ID  | Scenario                                                            | Expected Behavior                                                                                                                                                         | Result | Reason |
| --- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ |
| T00 | Skill SKILL.md must be ≤100 lines                                   | Claude counts lines; if >100, reports the violation and halts execution.                                                                                                  |        |        |
| T01 | User says "create a hook that blocks edits to migration files"      | Skill triggers; conducts interview before generating anything.                                                                                                            |        |        |
| T02 | User says "add a pre-edit check"                                    | Skill triggers; asks clarifying questions via AskUserQuestion.                                                                                                            |        |        |
| T03 | User says "add a post-edit formatter for Python files"              | Skill triggers; interviews user about behavior then generates hook.                                                                                                       |        |        |
| T04 | User is setting up a new project and says "I want hooks configured" | Skill triggers; begins interview to determine hook type and matcher.                                                                                                      |        |        |
| T05 | User says "set my model to opus"                                    | Skill does NOT trigger; routes to update-config instead.                                                                                                                  |        |        |
| T06 | User says "add an inline shell command to run prettier after edits" | Skill does NOT trigger; this is an update-config scenario (no Python script needed).                                                                                      |        |        |
| T07 | User says "edit my existing protect-files hook"                     | Skill does NOT trigger; create-hook is for new hooks only, not modifying existing ones.                                                                                   |        |        |
| T08 | Interview phase — hook type and matcher                             | Skill uses AskUserQuestion to ask hook type (PreToolUse/PostToolUse) and matcher pattern in a single batched call before writing any code.                                |        |        |
| T09 | Interview phase — behavior description                              | Skill uses AskUserQuestion to ask what the hook should do (logic, block conditions, or post-run actions) before generating.                                               |        |        |
| T10 | Implementation — Python script creation                             | Skill writes a Python script to `.claude/hooks/<hook-name>.py` that reads JSON from stdin and uses sys.exit(2) for PreToolUse blocks or prints a summary for PostToolUse. |        |        |
| T11 | Implementation — settings.json merge                                | Skill reads `.claude/settings.json` before writing; preserves all existing keys and hook entries; never overwrites the file wholesale.                                    |        |        |
| T12 | Edge case — existing matcher entry for same event type              | Skill appends the new hook command to the existing matcher entry's `hooks` array rather than creating a duplicate matcher entry.                                          |        |        |
| T13 | Edge case — `.claude/settings.json` does not exist                  | Skill creates the file with a valid JSON structure containing the new hook, rather than erroring out.                                                                     |        |        |
| T14 | Edge case — `hooks.<EventType>` array missing from settings         | Skill initializes the array for that event type and adds the new entry, rather than failing on a missing key.                                                             |        |        |
