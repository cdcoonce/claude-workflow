# Test Suite — create-hook

| ID  | Scenario                                                            | Expected Behavior                                                                                                                                                         | Result | Reason                                                                                                                                                                   |
| --- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T00 | Skill SKILL.md must be ≤100 lines                                   | Claude counts lines; if >100, reports the violation and halts execution.                                                                                                  | pass   | The skill opens with "Self-check: Count the lines in this file. If the count exceeds 100, report the violation and halt execution."                                      |
| T01 | User says "create a hook that blocks edits to migration files"      | Skill triggers; conducts interview before generating anything.                                                                                                            | pass   | This phrase matches the Trigger examples list verbatim, and the Interview section precedes Implementation.                                                               |
| T02 | User says "add a pre-edit check"                                    | Skill triggers; asks clarifying questions via AskUserQuestion.                                                                                                            | pass   | "add a pre-edit check" is listed explicitly in Trigger examples, and the Interview section specifies using AskUserQuestion before any generation.                        |
| T03 | User says "add a post-edit formatter for Python files"              | Skill triggers; interviews user about behavior then generates hook.                                                                                                       | pass   | "add a post-edit formatter for Python files" is listed verbatim in Trigger examples, and Interview precedes Implementation in the skill flow.                            |
| T04 | User is setting up a new project and says "I want hooks configured" | Skill triggers; begins interview to determine hook type and matcher.                                                                                                      | fail   | The Scope section classifies "I want hooks configured" as vague language and instructs Claude to ask a single clarifying question first — not launch the full interview. |
| T05 | User says "set my model to opus"                                    | Skill does NOT trigger; routes to update-config instead.                                                                                                                  | pass   | Non-trigger examples states "set my model to opus → update-config" explicitly.                                                                                           |
| T06 | User says "add an inline shell command to run prettier after edits" | Skill does NOT trigger; this is an update-config scenario (no Python script needed).                                                                                      | pass   | Non-trigger examples states "add an inline shell command to run prettier → update-config" explicitly.                                                                    |
| T07 | User says "edit my existing protect-files hook"                     | Skill does NOT trigger; create-hook is for new hooks only, not modifying existing ones.                                                                                   | pass   | The Scope section states "Requests to edit or modify an existing hook — respond that create-hook is for new hooks only and stop."                                        |
| T08 | Interview phase — hook type and matcher                             | Skill uses AskUserQuestion to ask hook type (PreToolUse/PostToolUse) and matcher pattern in a single batched call before writing any code.                                | pass   | Call 1 instructs "Ask these together" covering hook type and matcher pattern, and Interview precedes Implementation.                                                     |
| T09 | Interview phase — behavior description                              | Skill uses AskUserQuestion to ask what the hook should do (logic, block conditions, or post-run actions) before generating.                                               | pass   | Call 2 asks "What should the hook do?" plus conditional questions for PreToolUse/PostToolUse, all before Implementation.                                                 |
| T10 | Implementation — Python script creation                             | Skill writes a Python script to `.claude/hooks/<hook-name>.py` that reads JSON from stdin and uses sys.exit(2) for PreToolUse blocks or prints a summary for PostToolUse. | pass   | Step 1 specifies the path, "Read JSON from stdin", sys.exit(2) for PreToolUse, and print summary for PostToolUse.                                                        |
| T11 | Implementation — settings.json merge                                | Skill reads `.claude/settings.json` before writing; preserves all existing keys and hook entries; never overwrites the file wholesale.                                    | pass   | Step 2 instructs "Read and parse the existing JSON" before modification; matcher-based merge preserves existing entries.                                                 |
| T12 | Edge case — existing matcher entry for same event type              | Skill appends the new hook command to the existing matcher entry's `hooks` array rather than creating a duplicate matcher entry.                                          | pass   | Step 2 states "If found: append the new hook command to that entry's `hooks` array."                                                                                     |
| T13 | Edge case — `.claude/settings.json` does not exist                  | Skill creates the file with a valid JSON structure containing the new hook, rather than erroring out.                                                                     | pass   | Step 2 states "If `.claude/settings.json` does not exist, create it with the structure `{\"hooks\": {}}` before proceeding."                                             |
| T14 | Edge case — `hooks.<EventType>` array missing from settings         | Skill initializes the array for that event type and adds the new entry, rather than failing on a missing key.                                                             | pass   | Step 2 states "If `hooks.<HookType>` key is absent, initialize it as an empty array `[]`."                                                                               |
