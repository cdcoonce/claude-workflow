# Test Suite — improve-skill

| ID  | Scenario                                                                                 | Expected Behavior                                                                                                                                                            | Result | Reason                                                                                                                                                                                      |
| --- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T00 | Skill SKILL.md must be ≤100 lines                                                        | Claude counts lines; if >100, reports the violation and halts execution.                                                                                                     | fail   | The SKILL.md contains no instruction for Claude to count its own lines or halt if >100; T00 is a test-suite convention, not a runtime self-check in the skill.                              |
| T01 | Slug is provided via `/improve-skill <slug>` and matches `core/skills/{slug}/SKILL.md`   | Resolves skill path correctly and records it in `skill_path`; proceeds to state file check.                                                                                  | pass   | Phase 1 Step 2 states: "Check `core/skills/{slug}/SKILL.md`... Record resolved path as `skill_path`."                                                                                       |
| T02 | Slug provided does not match any skill in `core/skills/` or `presets/*/skills/`          | Aborts with: "Error: no skill found for slug "{slug}". Searched core/skills/{slug}/SKILL.md and presets/\*/skills/{slug}/SKILL.md."                                          | pass   | Phase 1 Step 2 includes the exact abort message.                                                                                                                                            |
| T03 | `/improve-skill` invoked with no slug argument                                           | Replies with usage instructions and stops — does not proceed to any phase.                                                                                                   | pass   | Phase 1 Step 1 states: "If no slug given, reply with usage and stop."                                                                                                                       |
| T04 | State file exists with `status: completed` or `status: abandoned`                        | Notifies user, starts a new run using `-2` (or `-3`, etc.) suffix to avoid collision.                                                                                        | pass   | Phase 1 Step 3: "Notify user, start new run (suffix slug with `-2`, `-3` etc.)."                                                                                                            |
| T05 | State file exists with `status: in_progress`                                             | Reports "Resuming {slug} at phase {current_phase} (best score: {best_score}%)." and jumps directly to that phase.                                                            | pass   | Phase 1 Step 3: "Report: 'Resuming **{slug}** at phase **{current_phase}** (best score: {best_score}%).' Jump to that phase."                                                               |
| T06 | Phase 2 Grill — skill-analyst agent is dispatched                                        | The skill-analyst agent is dispatched with the full SKILL.md content; a three-tier weakness report (Surface Gaps, Behavioral Edge Cases, Adversarial Probes) is received.    | pass   | phase-2-grill.md Step 1: "Adopt the `skill-analyst` agent identity. Provide the full SKILL.md content pasted inline." and "Receive: a three-tier weakness report."                          |
| T07 | Phase 2 Grill — AskUserQuestion tool is NOT available for challenge loop                 | Falls back to numbered text listing of findings per tier; user types rejection numbers and additions. Does not error or skip tiers.                                          | pass   | phase-2-grill.md Step 2: "If AskUserQuestion is NOT available: List findings numbered, ask user to type which numbers to reject and any additions."                                         |
| T08 | Phase 2 Grill — no tests.md exists yet                                                   | Skill-analyst is dispatched without existing test summaries; findings are assembled into a new tests.md with T00 first.                                                      | pass   | phase-2-grill.md Step 1 implies no summaries if tests_path doesn't exist; Step 3 new suite: "T00 is always first."                                                                          |
| T09 | Phase 2 Grill — tests.md already exists with N rows                                      | Skill-analyst receives existing test summaries to identify uncovered gaps; new rows are appended only — never removes or renumbers existing rows.                            | pass   | phase-2-grill.md Step 1: "also read it, extract one-line summaries." Step 3 append mode: "Do not remove, modify, or renumber existing rows."                                                |
| T10 | Phase 2 Grill — T00 row placement                                                        | T00 ("Skill SKILL.md must be ≤100 lines") is always the first data row in any newly created tests.md; never duplicated if suite already contains it.                         | pass   | phase-2-grill.md Step 3: "T00 is always first" and "Do not duplicate T00."                                                                                                                  |
| T11 | Phase 3 Baseline — QA Tester agent scores the original skill                             | Baseline score is recorded in the state file under `baseline_score` and as iteration 0 in the Scores table; `best_score` is initialized to this value.                       | pass   | phase-3-baseline.md Step 4: "Set `baseline_score: {pct}`" and "Add a row to the Scores table." SKILL.md: "`best_score` is initialized to `baseline_score`."                                 |
| T12 | Phase 4 Iterate — Skill Writer rewrites, QA Tester rescores, loop repeats                | Skill Writer and QA Tester execute as a team across multiple iterations (not a single pass); loop continues until target pass rate is met or max iterations is reached.      | pass   | SKILL.md Phase 4: "The Skill Writer and QA Tester execute as a team across multiple iterations... Loop continues until target pass rate is met or max iterations is reached."               |
| T13 | Phase 4 Iterate — two consecutive iterations with no score improvement                   | Stall detected: strategy agent is invoked to propose a rewrite strategy before the next Skill Writer iteration begins.                                                       | pass   | SKILL.md Phase 4: "When `stall_count` >= 2, invoke the strategy agent to propose a rewrite strategy."                                                                                       |
| T14 | Phase 4 Iterate — iteration produces a score lower than the current best                 | Best version is preserved; state file retains `best_score` and `best_iteration` from the previous peak; regression is logged.                                                | pass   | SKILL.md Phase 4: "if score < `best_score` (regression), log the regression, preserve `best_score` and `best_iteration`, and revert to the best known SKILL.md."                            |
| T15 | Phase 5 Finalize — improvement run completes                                             | A PR is filed, the state file `status` is set to `completed`, state file is archived to `docs/archive/skill-improve/`, and a score report (baseline → best) is produced.     | pass   | SKILL.md Phase 5 summary matches all expected outcomes.                                                                                                                                     |
| T16 | Phase 2 Grill — zero accepted findings after challenge loop (excluding T00)              | Skill re-prompts once asking for a test count; does not proceed with 0 tests beyond T00.                                                                                     | pass   | phase-2-grill.md Step 3: "If the count would be 0 (excluding T00), re-prompt once."                                                                                                         |
| T17 | Phase 2 Grill — commit step after tests.md is written                                    | `git add {tests_path} docs/skill-improve/{slug}.state.md` and `git commit -m "test({slug}): add benchmark test suite"` are executed.                                         | pass   | phase-2-grill.md Step 5: exact commands specified.                                                                                                                                          |
| T18 | Slug resolves via `presets/*/skills/{slug}/SKILL.md` (preset path)                       | Correctly records the preset path as `skill_path` and derives `tests_path` from that directory.                                                                              | pass   | Phase 1 Step 2: "Check `core/skills/{slug}/SKILL.md`, then `presets/*/skills/{slug}/SKILL.md`... Record resolved path as `skill_path`."                                                     |
| T19 | State file collision — both `{slug}.state.md` and `{slug}-2.state.md` exist as completed | Third run uses `-3` suffix; does not overwrite or reuse existing completed state files.                                                                                      | pass   | Phase 1 Step 3: "suffix slug with `-2`, `-3` etc." implies incremental suffixing.                                                                                                           |
| T20 | Phase 4 Iterate — target pass rate reached before max_iterations                         | Loop exits early upon hitting the target; does not continue iterating unnecessarily.                                                                                         | pass   | phase-4-iterate.md Exit Conditions: "`current_score >= target_pass_rate` → exit with reason 'target reached'."                                                                              |
| T21 | Phase 4 Iterate — max_iterations reached without hitting target pass rate                | Loop exits and proceeds to Phase 5 Finalize; does not hang, abort, or error.                                                                                                 | pass   | phase-4-iterate.md Exit Conditions: "`current_iteration >= max_iterations` → exit with reason 'budget exhausted'."                                                                          |
| T22 | Phase 4 Iterate — after strategy agent is dispatched                                     | `stall_count` is reset to 0 in the state file before the next Skill Writer iteration begins.                                                                                 | pass   | SKILL.md Phase 4: "then reset `stall_count` to 0." phase-4-iterate.md Step H: "Reset `stall_count` to `0`."                                                                                 |
| T23 | Phase 5 Finalize — best iteration is not the last iteration                              | PR uses the SKILL.md from `best_iteration`, not the most recent rewrite.                                                                                                     | pass   | SKILL.md Phase 5: "a PR is filed using the SKILL.md content from `best_iteration` (not the most recent rewrite)."                                                                           |
| T24 | Phase 2 Grill — skill-analyst returns malformed output                                   | Retries once; if still malformed, prompts user with "Skill analyst produced malformed output twice. Please inspect and advise before continuing."                            | pass   | phase-2-grill.md Step 1 Malformed output handling specifies retry once and exact user prompt.                                                                                               |
| T25 | Phase 2 Grill — user rejects all findings across all three tiers                         | Warns user, asks what gaps to look for, and re-runs the skill-analyst with the user's guidance passed as additional context; capped at 2 re-analyses before manual fallback. | pass   | phase-2-grill.md Step 2 Full-rejection guard covers all expected behaviors.                                                                                                                 |
| T26 | `references/` path resolved from non-repo-root CWD                                       | Claude reads the correct file at `core/skills/improve-skill/references/` regardless of CWD; does not resolve relative to CWD or target skill directory.                      | fail   | SKILL.md says "Read `references/phase-2-grill.md`" — a relative path with no anchoring instruction specifying resolution relative to the skill's own directory.                             |
| T27 | Resume flow — `skill_path` in state file points to a deleted file                        | Claude aborts with a clear error stating the skill file no longer exists; does not silently proceed to the recorded phase.                                                   | pass   | Phase 1 Step 3: "Verify `skill_path` exists on disk." Implicit abort on verification failure.                                                                                               |
| T28 | Phase 4 `.best_skill.md` sidecar for a preset-resolved skill                             | Sidecar is written to the same directory as the resolved preset skill (e.g., `presets/python-api/skills/tdd/`), not hardcoded to `core/skills/`.                             | fail   | phase-4-iterate.md Step G hardcodes: "Copy content to `core/skills/{slug}/.best_skill.md`" — always uses `core/skills/`, not the resolved preset directory.                                 |
| T29 | `prd_issue` field in state schema — dead code path                                       | Phase 1 never captures an issue number; `prd_issue` remains blank; Phase 5 `Closes #` line is not emitted when `prd_issue` is empty.                                         | pass   | Phase 1 has no step capturing `prd_issue`. phase-5-finalize.md Step 5: "If blank or absent, omit."                                                                                          |
| T30 | Phase 4 first iteration — `current_score` undefined at exit check                        | Claude uses `baseline_score` (equal to `best_score`) for the first exit-condition check; does not error or skip the check.                                                   | pass   | phase-4-iterate.md Initialization: "If `best_score` is blank... set `best_score` to `baseline_score`." Phase 3 validates baseline < target before entering Phase 4.                         |
| T31 | Phase 4 — SKILL.md vs reference doc disagree on regression stall_count                   | Claude follows SKILL.md: regressions increment `stall_count` by 1 AND revert to best version; the two behaviors are not mutually exclusive.                                  | fail   | SKILL.md says regressions increment stall_count, but phase-4-iterate.md Step G Regression only reverts without incrementing; only "No change" increments. The documents contradict.         |
| T32 | Phase 5 — report data destroyed before score report generation                           | Claude reads the annotated test table (Result/Reason) BEFORE clearing it in Step 2, so Step 3 can accurately report tests fixed and tests still failing.                     | fail   | phase-5-finalize.md Step 2 clears Result/Reason columns, then Step 3 needs that data for "Tests fixed" and "Tests still failing" sections. No instruction to preserve data before clearing. |
| T33 | Path traversal slug (e.g., `../../etc/passwd`)                                           | Claude rejects the slug as invalid (contains path separators or traversal sequences) and does not attempt file resolution or creation at that path.                          | fail   | No instruction validates slug characters or rejects path traversal sequences. Phase 1 Step 1 only checks "If no slug given."                                                                |
| T34 | Slug with git-invalid characters (spaces, `~`, `^`, `:`, `\`)                            | Claude rejects the slug as invalid before attempting branch creation; does not silently fail mid-pipeline.                                                                   | fail   | No instruction validates slug format for git-safe characters. Phase 1 has no slug sanitization step.                                                                                        |
| T35 | Phase 3 — user provides non-numeric or out-of-range target pass rate                     | Claude rejects invalid input (e.g., "200%", "-5", "high"), explains valid range (integer between baseline_score+1 and 100), and re-prompts.                                  | fail   | Phase 2 grill Step 4: "Ask target pass rate (default 90%)" — no validation specified. Phase 3 Step 5 only validates when baseline >= target, not general input validation.                  |
| T36 | Phase 5 — PR branch has merge conflicts with default branch                              | Claude detects conflicts and warns the user that manual resolution is needed; does not silently open an unmergeable PR.                                                      | fail   | phase-5-finalize.md Step 5 runs `gh pr create` directly — no instruction to check for merge conflicts or warn the user.                                                                     |
