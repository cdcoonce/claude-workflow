# Test Suite — improve-skill

| ID  | Scenario                                                                                 | Expected Behavior                                                                                                                                                        | Result | Reason                                                                                                                                                                                                               |
| --- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T00 | Skill SKILL.md must be ≤100 lines                                                        | Claude counts lines; if >100, reports the violation and halts execution.                                                                                                 | pass   | The skill is 82 lines, which is under the 100-line limit; no violation to report.                                                                                                                                    |
| T01 | Slug is provided via `/improve-skill <slug>` and matches `core/skills/{slug}/SKILL.md`   | Resolves skill path correctly and records it in `skill_path`; proceeds to state file check.                                                                              | pass   | Phase 1 Step 2 instructs "Check `core/skills/{slug}/SKILL.md`... Record resolved path as `skill_path`" and Step 3 instructs checking the state file next.                                                            |
| T02 | Slug provided does not match any skill in `core/skills/` or `presets/*/skills/`          | Aborts with: "Error: no skill found for slug "{slug}". Searched core/skills/{slug}/SKILL.md and presets/\*/skills/{slug}/SKILL.md."                                      | pass   | Phase 1 Step 2 provides the exact abort message verbatim.                                                                                                                                                            |
| T03 | `/improve-skill` invoked with no slug argument                                           | Replies with usage instructions and stops — does not proceed to any phase.                                                                                               | pass   | Phase 1 Step 1 states "If no slug given, reply with usage and stop."                                                                                                                                                 |
| T04 | State file exists with `status: completed` or `status: abandoned`                        | Notifies user, starts a new run using `-2` (or `-3`, etc.) suffix to avoid collision.                                                                                    | pass   | Phase 1 Step 3 states "`status: completed` or `status: abandoned`: Notify user, start new run (suffix slug with `-2`, `-3` etc.)."                                                                                   |
| T05 | State file exists with `status: in_progress`                                             | Reports "Resuming {slug} at phase {current_phase} (best score: {best_score}%)." and jumps directly to that phase.                                                        | pass   | Phase 1 Step 3 states the exact report format and "Jump to that phase."                                                                                                                                              |
| T06 | Phase 2 Grill — AskUserQuestion tool is available                                        | All interview questions are posed using AskUserQuestion tool with selectable options; no plain-text numbered Q&A is used.                                                | pass   | Phase 2 Step 2 states "conduct the interview using AskUserQuestion if available" and the available branch specifies it is used "for every question with selectable options."                                         |
| T07 | Phase 2 Grill — AskUserQuestion tool is NOT available                                    | Falls back gracefully to numbered text Q&A (1–6 questions in sequence); does not error or skip questions.                                                                | pass   | Phase 2 Step 2 states "otherwise numbered text Q&A" and the unavailable branch specifies "Ask questions 1–6 in sequence as numbered plain text."                                                                     |
| T08 | Phase 2 Grill — no tests.md exists yet                                                   | Claude reads the skill's SKILL.md and infers recommended test cases, presenting them as selectable options or suggestions before asking user for additions.              | pass   | Phase 2 Step 2 states "First, read `{skill_path}` and infer 5–10 suggested test cases from its content"; Q5 instructs presenting them as selectable options.                                                         |
| T09 | Phase 2 Grill — tests.md already exists with N rows                                      | Shows a one-line summary of each existing test; asks what harder or missing cases to add; appends new rows only — never removes or renumbers existing rows.              | pass   | Phase 2 Step 1 states "count data rows, show one-line summaries. Ask user what harder or missing cases to add. Append new rows only (never remove; never duplicate T00)."                                            |
| T10 | Phase 2 Grill — T00 row placement                                                        | T00 ("Skill SKILL.md must be ≤100 lines") is always the first data row in any newly created tests.md; never duplicated if suite already contains it.                     | pass   | Phase 2 Step 2 states "T00 is always first" with the prescribed scenario text; Step 1 states "never duplicate T00" for the existing-file path.                                                                       |
| T11 | Phase 3 Baseline — QA Tester agent scores the original skill                             | Baseline score is recorded in the state file under `baseline_score` and as iteration 0 in the Scores table; `best_score` is initialized to this value.                   | pass   | Phase 3 states "`baseline_score` is recorded in state under `baseline_score`; iteration 0 is added to the Scores table; `best_score` is initialized to `baseline_score`."                                            |
| T12 | Phase 4 Iterate — Skill Writer rewrites, QA Tester rescores, loop repeats                | Skill Writer and QA Tester execute as a team across multiple iterations (not a single pass); loop continues until target pass rate is met or max iterations is reached.  | pass   | Phase 4 states "The Skill Writer and QA Tester execute as a team across multiple iterations" and "Loop continues until target pass rate is met or max iterations is reached."                                        |
| T13 | Phase 4 Iterate — two consecutive iterations with no score improvement                   | Stall detected: strategy agent is invoked to propose a rewrite strategy before the next Skill Writer iteration begins.                                                   | pass   | Phase 4 states "When `stall_count` >= 2, invoke the strategy agent"; stall_count increments by 1 on each tie or regression, so two consecutive non-improvements trigger the condition.                               |
| T14 | Phase 4 Iterate — iteration produces a score lower than the current best                 | Best version is preserved; state file retains `best_score` and `best_iteration` from the previous peak; regression is logged.                                            | pass   | Phase 4 states "if score < `best_score` (regression), log the regression, preserve `best_score` and `best_iteration`, and revert to the best known SKILL.md."                                                        |
| T15 | Phase 5 Finalize — improvement run completes                                             | A PR is filed, the state file `status` is set to `completed`, state file is archived to `docs/archive/skill-improve/`, and a score report (baseline → best) is produced. | pass   | Phase 5 states all four outcomes: PR filed, status set to `completed`, archived to `docs/archive/skill-improve/`, and score report produced showing baseline → best score.                                           |
| T16 | Phase 2 Grill — user enters 0 for test count                                             | Skill re-prompts once asking for a count; does not proceed with 0 tests.                                                                                                 | pass   | Phase 2 Step 2 states "If user answered 0 for test count, re-prompt once."                                                                                                                                           |
| T17 | Phase 2 Grill — commit step after tests.md is written                                    | `git add {tests_path} docs/skill-improve/{slug}.state.md` and `git commit -m "test({slug}): add benchmark test suite"` are executed.                                     | pass   | Phase 2 Step 4 states the exact command verbatim.                                                                                                                                                                    |
| T18 | Slug resolves via `presets/*/skills/{slug}/SKILL.md` (preset path)                       | Correctly records the preset path as `skill_path` and derives `tests_path` from that directory.                                                                          | pass   | Phase 1 Step 2 states "Check `core/skills/{slug}/SKILL.md`, then `presets/*/skills/{slug}/SKILL.md`... Record resolved path as `skill_path`. Derive `tests_path` as the directory of `skill_path` plus `/tests.md`." |
| T19 | State file collision — both `{slug}.state.md` and `{slug}-2.state.md` exist as completed | Third run uses `-3` suffix; does not overwrite or reuse existing completed state files.                                                                                  | pass   | Phase 1 Step 3 states "suffix slug with `-2`, `-3` etc." — the pattern continues incrementing past `-2` until a free suffix is found.                                                                                |
| T20 | Phase 4 Iterate — target pass rate reached before max_iterations                         | Loop exits early upon hitting the target; does not continue iterating unnecessarily.                                                                                     | pass   | Phase 4 states "Loop continues until target pass rate is met or max iterations is reached" — meeting the target ends the loop immediately.                                                                           |
| T21 | Phase 4 Iterate — max_iterations reached without hitting target pass rate                | Loop exits and proceeds to Phase 5 Finalize; does not hang, abort, or error.                                                                                             | pass   | Phase 4 states "Loop continues until target pass rate is met or max iterations is reached" — hitting max iterations ends the loop; Phase 5 Finalize follows as the next phase in sequence.                           |
| T22 | Phase 4 Iterate — after strategy agent is dispatched                                     | `stall_count` is reset to 0 in the state file before the next Skill Writer iteration begins.                                                                             | pass   | Phase 4 states "invoke the strategy agent to propose a rewrite strategy before the next Skill Writer iteration begins, then reset `stall_count` to 0."                                                               |
| T23 | Phase 5 Finalize — best iteration is not the last iteration                              | PR uses the SKILL.md from `best_iteration`, not the most recent rewrite.                                                                                                 | pass   | Phase 5 states "a PR is filed using the SKILL.md content from `best_iteration` (not the most recent rewrite)."                                                                                                       |
